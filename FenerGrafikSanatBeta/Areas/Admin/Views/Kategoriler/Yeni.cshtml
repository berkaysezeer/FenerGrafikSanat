@model FenerGrafikSanatBeta.Models.Kategori
@{
    ViewBag.Title = "Yeni Kategori";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-6">
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">Yeni Kategori Ekle</h3>
                </div>
                <!-- /.card-header -->
                <!-- form start -->
                @using (Html.BeginForm("Yeni", "Kategoriler", FormMethod.Post, new { role = "form" }))
                {
                    @Html.AntiForgeryToken()
                    <div class="card-body">
                        <div class="form-group">
                            @Html.LabelFor(x => x.KategoriAd)
                            @Html.TextBoxFor(x => x.KategoriAd, new { @class = "form-control", placeholder = "Kategori Adı" })
                            @Html.ValidationMessageFor(x => x.KategoriAd, null, new { @class = "text-danger" })
                        </div>
                        <div class="form-group">
                            @Html.LabelFor(x => x.Slug)

                            <div class="input-group">
                                @Html.TextBoxFor(x => x.Slug, new { @class = "form-control", placeholder = "Url Adresi" })
                                <div class="input-group-append">
                                    <a id="refreshSlug" href="#" class="btn btn-outline-secondary">
                                        <i class="fas fa-sync-alt"></i>
                                    </a>
                                </div>
                            </div>

                            @Html.ValidationMessageFor(x => x.Slug, null, new { @class = "text-danger" })
                        </div>
                    </div>
                    <!-- /.card-body -->

                    <div class="card-footer">
                        <a href="@Url.Action("Index")" class="btn btn-secondary">İptal</a>
                        <button type="submit" class="btn btn-primary">Ekle</button>
                    </div>
                }

            </div>
            <!-- /.card -->
        </div>
    </div>
</div>

@section scripts {
    <script>
        slug.run("#KategoriAd", "#Slug", "#refreshSlug");
    </script>
}